import Toaster from './toaster'
import {ToastsWrapper, WrapperAnimated, Wrapper, Alert} from './styles'

export {Toast} from './toast'
export const toaster = new Toaster()
export const ToastElements = {
  ToastsWrapper,
  WrapperAnimated,
  Wrapper,
  Alert
}
