import * as React from 'react'
import {AutocompleteItemProps} from './types'

export const AutocompleteItem:React.FC<AutocompleteItemProps> = ({isHighlited,isSelected,children,...props}) => {
    return (
        <div>hello</div>
    )
}