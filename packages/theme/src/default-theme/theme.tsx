import {text, headings, fontFamilies} from "./typography"
import spacing from "./spacing"
import {colors} from "./foundational-styles"

export const theme = {
  style: {
    text,
    headings
  },
  spacing,
  colors,
  fontFamilies,
  radius: "5px"
}
